scale=20; a=1; b=1/sqrt(2); t=0.25; p=1;

        a_next=(a+b)/2;
        b=sqrt(a*b);
        t=t-p*(a-a_next)^2;
        p=2*p;
        a=a_next;

        a_next=(a+b)/2;
        b=sqrt(a*b);
        t=t-p*(a-a_next)^2;
        p=2*p;
        a=a_next;

        a_next=(a+b)/2;
        b=sqrt(a*b);
        t=t-p*(a-a_next)^2;
        p=2*p;
        a=a_next;
(a+b)^2/(4*t)
